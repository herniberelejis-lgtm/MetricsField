name: Verificación

# Corre el mismo ciclo que se pide antes de cada commit (ver CLAUDE.md):
# tipos + tests + build. Hasta ahora eso dependía de que cada uno se
# acordara; con esto, un PR que rompe algo no se puede fusionar sin que
# quede a la vista.
#
# No necesita ningún secreto: el build de Next no se conecta a la base
# (todas las páginas son force-dynamic, se renderizan en request) y los
# tests son de lógica pura. DATABASE_URL y LOYALTY_DATABASE_URL se pasan
# en falso solo porque lib/sql.ts y lib/sql-loyalty.ts cortan el arranque
# si la variable no existe — ninguna de las dos intenta conectar de
# verdad durante el build.

on:
  pull_request:
    branches: [main, desarrollo]
  push:
    branches: [main, desarrollo]

jobs:
  verificar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Instalar dependencias
        run: npm ci

      - name: Chequeo de tipos
        run: npx tsc --noEmit

      - name: Tests
        run: npm test

      - name: Build de producción
        run: npm run build
        env:
          DATABASE_URL: postgres://build:build@localhost:5432/build
          LOYALTY_DATABASE_URL: postgres://build:build@localhost:5432/build
          ADMIN_PASSWORD: build-no-se-usa

      - name: Vulnerabilidades en dependencias
        run: npm audit --audit-level=high
